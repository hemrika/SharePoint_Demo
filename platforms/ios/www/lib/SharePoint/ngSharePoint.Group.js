(function () {
  'use strict';

  var SharePoint = angular.module('ngSharePoint');

  SharePoint.factory('ngGroup', ['$resource', '$q', function ($resource, $q) {
    var ngGroup = {
    };

    return ngGroup;
  }]);

})();
