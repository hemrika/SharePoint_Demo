(function () {
  'use strict';

  var SharePoint = angular.module('ngSharePoint');

  SharePoint.factory('ngTypes', ['$resource', '$q', function ($resource, $q) {
    var ngTypes = {
    };

    return ngTypes;
  }]);

})();
